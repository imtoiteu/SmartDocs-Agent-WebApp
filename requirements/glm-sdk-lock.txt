# GLM-OCR SDK lock — for GLM-OCR/.venv-sdk
# ---------------------------------------------------------------------------
# Pinned deps for the glmocr CLI venv (layout detector + SDK pipeline). This is
# the venv the SmartDocs UI actually uses: services/ocr_engines/glm_adapter.py
# runs `GLM_SDK_PYTHON -m glmocr.cli parse ...` as a SUBPROCESS, and config.py
# resolves GLM_SDK_PYTHON to .venv-sdk (preferred) before .venv-mlx.
#
# The glmocr package itself is installed separately via editable install
# (pip install -e "$GLM_OCR_DIR" --no-deps) by scripts/setup_glm.sh — NOT listed
# here, so there is no stale absolute path baked into this file.
#
# torch/torchvision below are macOS/arm64 builds. On Linux, pip resolves the
# Linux CPU wheels for the same version — this is expected.
# For CUDA on Linux: pip install torch --index-url https://download.pytorch.org/whl/cu121
#   BEFORE running setup_glm.sh.
#
# Pillow==12.2.0 is intentional. This venv is ISOLATED from the main SmartDocs
# venv (Pillow 10.2.0 + vietocr). No conflict between the two venvs.
# ---------------------------------------------------------------------------
PyMuPDF==1.28.0
PyYAML==6.0.3
Pygments==2.20.0
accelerate==1.14.0
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.0
certifi==2026.5.20
charset-normalizer==3.4.7
click==8.4.1
filelock==3.29.4
fsspec==2026.6.0
h11==0.16.0
hf-xet==1.5.1
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.19.0
idna==3.18
markdown-it-py==4.2.0
mdurl==0.1.2
mpmath==1.3.0
networkx==3.6.1
numpy==2.4.6
opencv-python-headless==4.13.0.92
packaging==26.2
pillow==12.2.0
portalocker==3.2.0
psutil==7.2.2
pydantic==2.13.4
pydantic_core==2.46.4
pypdfium2==5.10.1
python-dotenv==1.2.2
regex==2026.5.9
requests==2.34.2
rich==15.0.0
safetensors==0.8.0
sentencepiece==0.2.1
setuptools>=78.1.0
shellingham==1.5.4
sympy==1.14.0
tokenizers==0.22.2
torch==2.12.0
torchvision==0.27.0
tqdm==4.68.3
transformers==5.12.1
typer==0.25.1
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.7.0
